<?php
namespace Monorepo\Test\Config;
/*
use Monorepo\Config\PackageConfig;
use PHPUnit\Framework\TestCase;
use RuntimeException;

class PackageConfigTest extends TestCase
{
    //======
    // @name
    //======
    public function testNameCanNotBeEmpty()
    {
        $config = new PackageConfig();

        $this->setExpectedException(RuntimeException::class, "Attempt to call getName with no name.");

        $config->getName();
    }

    public function testNameCanNotBeSetTwice()
    {
        $config = new PackageConfig();

        $this->setExpectedException(RuntimeException::class, "Attempt to call setName more than once.");

        $config->setName("name");
        $config->setName("name");
    }

    public function testNameWorks()
    {
        $config = new PackageConfig();

        $config->setName("Test Name");

        $this->assertEquals("Test Name", $config->getName());
    }

    //======
    // @path
    //======
    public function testPathCanNotBeEmpty()
    {
        $config = new PackageConfig();

        $this->setExpectedException(RuntimeException::class, "Attempt to call getPath with no path.");

        $config->getPath();
    }

    public function testPathCanNotBeSetTwice()
    {
        $config = new PackageConfig();

        $this->setExpectedException(RuntimeException::class, "Attempt to call setPath more than once.");

        $config->setPath("path");
        $config->setPath("path");
    }

    public function testPathWorks()
    {
        $config = new PackageConfig();

        $config->setPath("Test Path");

        $this->assertEquals("Test Path", $config->getPath());
    }

    //========
    // @static
    //========
}
*/
